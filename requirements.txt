# Always & Furever Donor CRM — Python dependencies
# Pinned to the versions verified in this environment.
Flask==3.1.3
Werkzeug==3.1.8

# Production WSGI server (do NOT use the Flask dev server on the web).
# Run with:  gunicorn -w 3 -b 127.0.0.1:8000 app:app
gunicorn==23.0.0
